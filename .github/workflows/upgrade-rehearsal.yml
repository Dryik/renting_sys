# Proves that a real 0.3.9 installation survives both electron-updater and a
# manual installer upgrade to the version on this branch, with its data intact.
#
# This workflow installs a package, replaces it through each supported path,
# and migrates the database the installed application opens. That is
# destructive, so it runs only on fresh GitHub-hosted Windows runners, which
# are discarded afterwards.
# The rehearsal itself refuses to start unless it can independently confirm the
# machine is disposable; the environment variable below is one half of that,
# and the runner's own identity is the other.
#
# It never publishes a release and never creates a tag.
name: Upgrade rehearsal

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "package.json"
      - "package-lock.json"
      - "electron/db/**"
      - "scripts/upgrade-rehearsal/**"
      - ".github/workflows/upgrade-rehearsal.yml"

# One workflow run per ref at a time. The matrix jobs use separate disposable
# runners, so the updater and manual-installer paths cannot share app data.
concurrency:
  group: upgrade-rehearsal-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  rehearse:
    # Hosted Windows only. The guard refuses anywhere else, but failing here is
    # a clearer signal than failing three steps later.
    runs-on: windows-latest
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        method: [updater, manual-installer]

    env:
      # Half of the disposable proof. The other half is the runner itself,
      # which the guard checks through GITHUB_ACTIONS, CI and RUNNER_TEMP.
      RENTAL_UPGRADE_REHEARSAL_DISPOSABLE: yes-this-machine-is-disposable
      RENTAL_UPGRADE_METHOD: ${{ matrix.method }}
      RENTAL_UPGRADE_REPORT: ${{ github.workspace }}\upgrade-rehearsal-report-${{ matrix.method }}.json

    steps:
      - name: Refuse to run anywhere but Windows
        shell: bash
        run: |
          if [ "$RUNNER_OS" != "Windows" ]; then
            echo "The rehearsal drives a Windows NSIS installer; this runner is $RUNNER_OS."
            exit 1
          fi

      - uses: actions/checkout@v4
        with:
          # The rehearsal builds the previous release from its own tag, so the
          # tag has to be in the clone.
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Unit tests
        run: npm test

      - name: Production audit
        run: npm audit --omit=dev

      - name: Whitespace check
        run: git diff --check

      - name: Build
        run: npm run build

      - name: Renderer smoke
        run: npm run smoke:renderer

      - name: Package this version
        run: npm run dist

      - name: Upgrade rehearsal
        run: npm run test:upgrade

      # Uploaded even on failure: the manifests and the update-event sequence
      # are the evidence, and a failed run is when they matter most.
      - name: Upload the rehearsal report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: upgrade-rehearsal-report-${{ matrix.method }}
          path: |
            upgrade-rehearsal-report-${{ matrix.method }}.json
            release/latest.yml
          if-no-files-found: warn
          retention-days: 14
